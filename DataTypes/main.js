//This is my first GitHub Practice
// console.log() is used to print (AKA log) information to the Command Line Interface (CLI) 
    // console.log("Hello World!")

//A variable is a symbol that stand for another value
//var x = 2;
//console.log(1+x);

// JavaScript Data types
    //1. Strings 
    // var name = "Spencer"
    // console.log("Hello, I am " + name);

    // var singleQuote = 'single-qouted';
    // var doubleQout = "double-qouted";
    // var backticks = `backticks`;

//back ticks can be used for more complicated stuff
    // var multiplePeople = `Spencer
    // Kyle
    // Cannon`

//The following are template literals
    // console.log(`My name is ${name}`)
    // console.log(`${multiplePeople}`)

    // We can reference a single letter by its index
    // var str = "Highness";
    // console.log('Index 2: ' + str[2] + '.'); // H
    // str(0) = "f" // wont work

    //Strings are immutable, which means that they cannot be change (however, variable can)
    // var newStr = "f" + str[1]

    // var str = "Sleepy"
    // console.log(str2[1] + str2[2] + str2[3])  
 //Sleepy
//012345

console.log('Hello World!'.replace('H','J'))

